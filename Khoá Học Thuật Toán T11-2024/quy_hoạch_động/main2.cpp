#include <bits/stdc++.h>

using namespace std;

int main() {
    // int n;
    // cin >> n;
    // vector<int> a(n);
    // for (int i = 0; i < n; i++) 
    //     cin >> a[i];
    // vector<int> dp(n);
    // dp[0] = 1;
    // for (int i = 1; i < n; i++) {
    //     if (a[i] == a[i - 1]) {
    //         dp[i] = dp[i - 1] + 1;
    //     } else {
    //         dp[i] = 1;
    //     }
    // }

    // int len = dp[0];
    // int end = 0;
    // for (int i = 1; i < n; i++) {
    //     if (dp[i] > len) {
    //         len = dp[i];
    //         end = i;
    //     }
    // }
    // cout << len << endl;
    // cout << a[end];

    int n = -7;
    cout << n % 2;
}