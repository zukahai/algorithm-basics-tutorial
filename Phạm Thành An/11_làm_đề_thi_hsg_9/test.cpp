#include <bits/stdc++.h>

using namespace std;

int main() {
    int n;
    // cin >> n;
    // vector<int> a(n);
    // map<int, int> m;
    // for (int i = 0; i < n; i++) {
    //     cin >> a[i];
    //     m[a[i]]++;
    // }

    // for (pair<int, int> p : m) {
    //     cout << p.first << " " << p.second << endl;
    // }

    pair<int, int> p = {7, 10};
    cout << p.first << " " << p.second << endl;

    return 0;
}