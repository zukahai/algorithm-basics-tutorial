#include <iostream>

using namespace std;
int fan(int x)
{
    for(i=1;i<x;i++)
        if(x%i==0)
        d++;
    if(d==2)
        return 1;
    return 0;

}
int main()
{
    freopen("FANDOM.INP","r",stdin);
    freopen("FANDOM.OUT","w",stdout);
    cout << "Hello world!" << endl;
    return 0;
}
